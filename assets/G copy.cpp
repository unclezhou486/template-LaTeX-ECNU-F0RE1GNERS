#include <bits/stdc++.h>

using namespace std;

set<int> pre[500010],suf[500010];

void solve(){
    int n,m;
    cin>>n>>m;
    vector<int> a(n+1);

    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    for(int i=1;i<=n;i++){
        
    }
}

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);
    solve();
    return 0;
}